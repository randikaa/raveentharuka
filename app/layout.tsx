import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Raveen Tharuka - Underground Electronic Music Producer & DJ",
  description:
    "Official website of Raveen Tharuka, underground electronic music producer and DJ from Sri Lanka. Book for events, listen to latest tracks, and explore the underground scene.",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
